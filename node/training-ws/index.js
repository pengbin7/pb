const axios = require("axios");
const request = require("request");
const { rejects } = require("assert");

async function login(usename){
    return new Promise((resolve,rejects)=>{
        request({},(err));
    })
}